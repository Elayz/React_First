import React, { useEffect, useState} from 'react';

export default class Timer{
  constructor() {
      const Timer = () => {
          const [time, setTime] = useState(0); // состояние для хранения времени
          const [isTimerRunning, setIsTimerRunning] = useState(false); // состояние для отслеживания запущен или остановлен таймер
      };
  }

}